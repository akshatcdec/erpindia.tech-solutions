@model List<ERPIndia.Models.SiteMenuModel>

<div class="row">
    @if (Model != null && Model.Count > 0)
    {
        for (int i = 0; i < Model.Count; i++)
        {
            <div class="col-3-xxxl col-xl-3">
                <div class="card account-settings-box height-auto">
                    <div class="card-body">
                        <div class="all-user-box">
                            <div class="media media-none--xs">
                                <div class="item-img">
                                    <i class='fa @Model[i].MenuImageName'></i>
                                </div>
                                <div class="media-body space-md">
                                    <h5 class="item-title"><a href='@Model[i].MenuPageName'>@Model[i].MenuName</a></h5>
                                </div>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        }
    }
 </div>


<div class="col-xs-12">
    <div class="box">
        <div class="box-header"><div class="message-box"></div></div>
        <div class="box-body">

            <div class="row">
                <div>
                    @if (Model != null && Model.Count > 0)
                    {
                        for (int i = 0; i < Model.Count; i++)
                        {
                            @*<div class='col-lg-3 col-md-3 col-sm-6 col-xs-12'>
                                <div class='dashboard-stat @Model[i].GetMenuColor'>
                                    <div class='visual'>
                                        <i class='fa @Model[i].MenuImageName'></i>
                                    </div>
                                    <div class='details'>
                                        <div class='number sitemap-box'>
                                            <a href='@Model[i].MenuPageName'>@Model[i].MenuName</a>
                                        </div>
                                        <div class='desc custome-sitemap-link'>
                                            @if (Model[i].SubMenuList != null && Model[i].SubMenuList.Count > 0)
                                            {
                                                <ul>
                                                    @for (int j = 0; j < Model[i].SubMenuList.Count; j++)
                                                    {
                                                        <li><a class="submenu" href='@Model[i].SubMenuList[j].MenuPageName'>@Model[i].SubMenuList[j].MenuName</a></li>
                                                    }
                                                </ul>
                                            }
                                        </div>
                                    </div>
                                </div>
                            </div>*@
                        }
                    }
                </div>
            </div>
        </div>
    </div>
</div>

<!-- END PAGE HEADER-->
